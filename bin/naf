#!/bin/sh
set -eu
root=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
if [ ! -x "$root/app/bin/naf" ]; then
    echo 'NAF console shortcut is missing. Run make composer-install first.' >&2
    exit 1
fi
exec "$root/app/bin/naf" "$@"
